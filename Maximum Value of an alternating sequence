class Solution {
public:
    long long maximumValue(int n, int s, int m) {
        long long peaks = n/2;
        if(peaks == 0)
            return s;

        return 1LL*s + m + (peaks -1)*(m -1);
        
    }
};©leetcode
